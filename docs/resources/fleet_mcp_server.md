---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "langsmith_fleet_mcp_server Resource - langsmith"
subcategory: ""
description: |-
  Registers and manages a fleet MCP server for the current workspace using the platform API (POST /v1/platform/fleet/mcp-servers https://api.smith.langchain.com/openapi.json, GET/PATCH/DELETE /v1/platform/fleet/mcp-servers/{mcp_server_id}). Only url, auth_type, oauth_provider_id, and headers can be changed in place; changing name, vendor_id, external_system_id, or oauth_mode forces replacement. Set headers to a JSON array of objects (for example [{"Authorization":"Bearer …"}]) matching the OpenAPI headers field.
---

# langsmith_fleet_mcp_server (Resource)

Registers and manages a **fleet MCP server** for the current workspace using the platform API ([`POST /v1/platform/fleet/mcp-servers`](https://api.smith.langchain.com/openapi.json), `GET`/`PATCH`/`DELETE` `/v1/platform/fleet/mcp-servers/{mcp_server_id}`). Only `url`, `auth_type`, `oauth_provider_id`, and `headers` can be changed in place; changing `name`, `vendor_id`, `external_system_id`, or `oauth_mode` forces replacement. Set `headers` to a JSON **array** of objects (for example `[{"Authorization":"Bearer …"}]`) matching the OpenAPI `headers` field.

## Example Usage

```terraform
resource "langsmith_fleet_mcp_server" "example" {
  name      = "Example MCP"
  url       = "https://mcp.example.com/sse"
  auth_type = "headers"
  headers   = jsonencode([{ Authorization = "Bearer replace-me" }])
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `name` (String) Display name for the MCP server (immutable after create).
- `url` (String) MCP server endpoint URL.

### Optional

- `auth_type` (String) Authentication mode: `headers` or `oauth`.
- `external_system_id` (String) Optional external system id (create-time only).
- `headers` (String) JSON array of header objects sent to the MCP server when using `auth_type = "headers"`.
- `oauth_mode` (String) OAuth registration mode: `legacy_shared_provider` or `per_user_dynamic_client` (create-time only).
- `oauth_provider_id` (String) OAuth provider id when `auth_type` is `oauth`.
- `vendor_id` (String) Optional vendor identifier supplied at registration time.

### Read-Only

- `can_invoke` (Boolean) Whether the current principal may invoke tools on this server (read-only).
- `created_at` (String) RFC3339 creation timestamp from the API.
- `id` (String) MCP server identifier returned by the API.
- `mcp_vendor_id` (String) Vendor linkage id materialized by LangSmith (read-only).
- `tenant_id` (String) Workspace tenant id owning the registration (read-only).
- `updated_at` (String) RFC3339 last-update timestamp from the API.
