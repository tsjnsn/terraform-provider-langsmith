---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "langsmith_settings Data Source - langsmith"
subcategory: ""
description: |-
  Reads the current workspace from GET /api/v1/settings (OpenAPI Tenant: id, display name, handle, created time). The workspace is whichever tenant the provider targets (tenant_id / LANGSMITH_TENANT_ID). To change the workspace handle, use the langsmith_settings resource (POST /api/v1/settings/handle).
---

# langsmith_settings (Data Source)

Reads the **current workspace** from `GET /api/v1/settings` (OpenAPI `Tenant`: id, display name, handle, created time). The workspace is whichever tenant the provider targets (`tenant_id` / `LANGSMITH_TENANT_ID`). To change the workspace handle, use the `langsmith_settings` resource (`POST /api/v1/settings/handle`).

## Example Usage

```terraform
data "langsmith_settings" "example" {}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Read-Only

- `created_at` (String) Workspace creation timestamp (RFC3339).
- `display_name` (String) The workspace display name.
- `id` (String) The workspace (tenant) UUID.
- `tenant_handle` (String) The workspace handle (slug), if set.
